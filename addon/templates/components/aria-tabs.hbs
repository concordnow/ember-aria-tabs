{{yield
  (hash
    tab=(component "aria-tab"
      selectedTabId=selectedTabId
      onTabClick=(action "onTabClick")
    )
    panel=(component "aria-panel"
      selectedPanelId=selectedPanelId
    )
  )
}}